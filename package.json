{
  "name": "@planning-center/icons",
  "version": "0.0.1",
  "description": "Planning Center Icons",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "optimize": "svgo ./accounts --multipass --config=svgo.yml && svgo ./app --multipass --config=svgo.color.yml && svgo ./check-ins --multipass --config=svgo.yml && svgo ./giving --multipass --config=svgo.yml && svgo ./groups --multipass --config=svgo.yml && svgo ./interfaces --multipass --config=svgo.yml && svgo ./people --multipass --config=svgo.yml && svgo ./planning-center --multipass --config=svgo.color.yml && svgo ./registrations --multipass --config=svgo.yml && svgo ./services --multipass --config=svgo.yml",
    "accessiblize": "node ./scripts/accessiblize.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/planningcenter/icons.git"
  },
  "author": "Michael Chan @chantastic",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/planningcenter/icons/issues"
  },
  "homepage": "https://github.com/planningcenter/icons#readme",
  "dependencies": {
    "cheerio": "^0.22.0",
    "svgo": "^0.7.2"
  }
}
