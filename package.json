{
  "name": "@planning-center/icons",
  "version": "1.0.0-pre-3",
  "description": "Planning Center Icons",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "optimize": "svgo ./svgs/accounts --multipass --config=svgo.yml && svgo ./svgs/apps --multipass --config=svgo.yml && svgo ./svgs/cco --multipass --config=svgo.yml && svgo ./svgs/check-ins --multipass --config=svgo.yml && svgo ./svgs/color-apps --multipass --config=svgo.color.yml && svgo ./svgs/giving --multipass --config=svgo.yml && svgo ./svgs/groups --multipass --config=svgo.yml && svgo ./svgs/interfaces --multipass --config=svgo.yml && svgo ./svgs/people --multipass --config=svgo.yml && svgo ./svgs/planning-center --multipass --config=svgo.color.yml && svgo ./svgs/registrations --multipass --config=svgo.yml && svgo ./svgs/services --multipass --config=svgo.yml",
    "accessiblize": "node ./scripts/accessiblize.js",
    "generate-components": "node ./scripts/generate-components.js",
    "prepublish": "yarn optimize && yarn accessiblize && yarn generate-components"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/planningcenter/icons.git"
  },
  "author": "Michael Chan @chantastic",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/planningcenter/icons/issues"
  },
  "homepage": "https://github.com/planningcenter/icons#readme",
  "dependencies": {
    "babel-core": "^6.22.1",
    "babel-preset-latest": "^6.22.0",
    "babel-preset-react": "^6.22.0",
    "cheerio": "^0.22.0",
    "lodash.camelcase": "^4.3.0",
    "lodash.upperfirst": "^4.3.1",
    "svgo": "^0.7.2"
  }
}
